```{julia}
include("./main_pkgs.jl")

```


```{julia}
fig = Figure()
# ax.aspect = DataAspect()
# x = LinRange(0, π, length(lon))
# y = LinRange(0, 2π, length(lat))
# z = LinRange(0, 3π, length(time))
sg = SliderGrid(fig[2, 1],
  (label="x", range=x),
  (label="y", range=y),
  (label="z", range=z)
)

sl_yz, sl_xz, sl_xy = sg.sliders
set_close_to!(sl_yz, 0.5length(x))
set_close_to!(sl_xz, 0.5length(y))
set_close_to!(sl_xy, 0.5length(z))

# ax = LScene(fig[1, 1], scenekw=(show_axis=true, tellheight=false, tellwidth=false))
# plt = volumeslices!(ax, x, y, z, vol)
# plt = volume(ax, x, y, z, vol)

# ax2 = LScene(fig[1, 2], scenekw=(show_axis=true,))
# plt2 = volume!(ax2, vol, algorithms=:absorption) # colormap = colormap, 
# plt = volume!(ax, x, y, z, vol, algorithms=:absorption)
# xlims!(ax, (1, 140))
# ylims!(ax, (1, 80))
# zlims!(ax, (1, 366))

## gap! not work
# colgap!(fig.layout, 200)
# rowgap!(fig.layout, 200)
fig
```
